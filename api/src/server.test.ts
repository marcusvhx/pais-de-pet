import { app } from "./server";
import { Request } from "supertest";

describe("teste do AppointmentController", () => {
  it("schedulling deve criar um agendamento do db e retornar os dados do agendamento", async () => {});
});
